#include "main.h"
/**
 * _strcpy - Pointing copied string to dest
 * @dest: Propably destination
 * @src: Probably source
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
int c;
for (i = 0; src[i] != '\0'; i++)
{
dest[i] = src[i];
dest[i] = '\0';
return (dest)
}
}
