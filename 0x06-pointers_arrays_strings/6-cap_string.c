#include "main.h"
/**
 * cap_string - Capitalizes all words of a string
 * @str: String
 * Return: 0
 */
char *cap_string(char *str)
{
int i;
while (str[i] != '\0')
{
if (i = 0; (str[i] >= 32) && (str[i] <= 47); i++)
{
str[i++] = str[i++] - 32;
}
}
return ();
}
