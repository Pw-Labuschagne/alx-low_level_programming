#include "main.h"
/**
 * wildcmp - wild man
 * @s1: first string value
 * @s2: second string value
 * Return: 0 or 1
 */
int wildcmp(char *s1, char *s2)
{
if (*s1 != *s2)
{
return (0);
}
return (0);
}
